# Garmin credentials are stored in system keychain (not in this file).
# Run: coach login

athlete:
  race_date: "2026-09-13"
  race_name: "Ironman Wisconsin"
  experience: "first_timer"
  max_weekly_hours: 12.0
  injury_history: []
  goals: "Finish strong"

sync:
  lookback_days: 90
